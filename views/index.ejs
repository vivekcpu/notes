<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
  <div class="main w-full min-h-screen bg-zinc-900">

    <h1 class="text-4xl text-white font-bold p-10 text-center underline">NOTES</h1>
    <p class="p-10 text-white text-center line-through  decoration-zinc-500 font-bolder">****CREATE YOUR NOTES SAVE, DELETE AND DO WHATEVER YOU WANT*****</p>
    
  <div class="form p-10 text-white">
    <form action="/create" method="post">
        <input class="block outline-none w-full rounded-md px-5 py-2 bg-zinc-800 " type="text" placeholder="Title....." name="title">
        <textarea class="block  outline-none w-full rounded-md mt-2 px-5 py-2 bg-zinc-800 resize-none" placeholder="Write your note here....." name="details"></textarea>
        <input class="mt-2 bg-blue-600 px-5 py-2 rounded-lg border-2 border-gray-300 active:border-green-500" type="submit" value="Create Task">
    </form>
  </div>

  <div class="tasks p-10 flex gap-3 flex-wrap">

<%if(files.length>0){%>
 
<% files.forEach(function(val){
  %>

   <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded-md">
   
        <h1 class="text-white  text-3xl trackig-tighter"><%= val%></h1>
        <a class="text-blue-500 inline-block mt-3" href="/files/<%= val%>">Read more</a>

        <form action="/delete/<%= val %>" method="POST" class="mt-2">
<button type="submit" class="bg-red-600 px-3 py-1 rounded-md text-white"
        onclick="return confirm('Are you sure you want to delete this note?');">
    Delete
</button>
        </form>

    </div>

<% }) %> 
<%}else{ %>
 <h2 class="text-zinc-600 px-2">No tasks Yet...</h2>
<%}%>

  </div>

  </div>

    <script src="/javascripts/script.js"></script> 
    <script src="/javascripts/disableInspect.js"></script>
 
</body> 
</html>